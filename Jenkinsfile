pipeline {
    stages {
        stage('Test') {
            steps {
                sh "npm --version"
            }
            steps {
                sh "npm --version"
            }
        }
    }
}
// test branch